"use client";

import React, { useState } from "react";
import { FaPlus, FaSearch } from "react-icons/fa";

const CustomerDashboard = () => {
  const [searchQuery, setSearchQuery] = useState("");

  const categories = [
    { name: "All Customers", count: null },
    { name: "Enterprise", count: 48 },
    { name: "Mid-Market", count: 64 },
    { name: "Small Business", count: 40 },
    { name: "Trial", count: 7 },
  ];

  return (
    <div className="p-6 bg-gray-50 min-h-screen">
      <h2 className="text-2xl font-semibold text-gray-800 mb-4">
        Customer Account Management
      </h2>

      {/* Statistics Cards */}
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-6">
        {[
          { label: "Total Customer Accounts", value: 152, change: "↑ 8%" },
          { label: "Active Accounts", value: 138, change: "↑ 5%" },
          { label: "Trial Accounts", value: 7, change: "↓ 12%", textColor: "text-red-500" },
          { label: "Service Usage (Monthly)", value: 7842, change: "↑ 15%" },
        ].map(({ label, value, change, textColor = "text-green-500" }) => (
          <div key={label} className="bg-white p-4 rounded-lg shadow-md">
            <p className="text-gray-600 text-sm">{label}</p>
            <p className="text-3xl font-bold text-gray-800">{value}</p>
            <p className={`text-sm ${textColor}`}>{change} vs last month</p>
          </div>
        ))}
      </div>

      {/* Category Tabs */}
      <div className="flex space-x-6 border-b pb-2 mb-4">
        {categories.map(({ name, count }) => (
          <div key={name} className="relative cursor-pointer text-gray-700 font-medium">
            {name} {count !== null && (
              <span className="ml-1 px-2 py-0.5 text-xs font-semibold bg-gray-200 rounded-full">{count}</span>
            )}
          </div>
        ))}
      </div>

      {/* Search & Filters */}
      <div className="flex flex-wrap items-center gap-4 mb-6">
        <div className="relative flex items-center">
          <input
            type="text"
            value={searchQuery}
            onChange={(e) => setSearchQuery(e.target.value)}
            placeholder="    Search customers..."
            className="border rounded-md p-2 pl-4 text-gray-700 focus:ring focus:ring-blue-300"
          />
          <button className="p-2 flex items-center bg-gray-200 border border-gray-300 rounded-r hover:bg-gray-300 transition-all focus:outline-none focus:ring-2 focus:ring-gray-400">
  <svg
    className="w-4 h-4 mr-1 text-gray-700"
    fill="currentColor"
    viewBox="0 0 512 512"
    aria-hidden="true"
  >
    <path d="M505 442.7l-99.7-99.7c28.4-35.3 45.7-80 45.7-128.7 0-114.9-93.1-208-208-208S35 99.4 35 214.3s93.1 
      208 208 208c48.7 0 93.4-17.3 128.7-45.7l99.7 99.7c4.5 4.5 10.6 6.7 16.7 6.7s12.2-2.2 16.7-6.7c9-9.2 
      9-24.3-.1-33.3zM243 362.3c-81.6 0-148-66.4-148-148s66.4-148 148-148 148 66.4 148 148-66.4 148-148 148z" />
  </svg>
  <span className="text-gray-700 text-sm font-medium">Search</span>
</button>

          
          <FaSearch className="absolute left-3 text-gray-400" />
        </div>

        <select className="border rounded-md p-2 text-gray-700">
          <option>All Statuses</option>
          <option>Active</option>
          <option>Inactive</option>
          <option>Trial</option>
        </select>

        <select className="border rounded-md p-2 text-gray-700">
          <option>All Plans</option>
          <option>Enterprise</option>
          <option>Professional</option>
          <option>Standard</option>
        </select>

        <button className="flex items-center gap-2 bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700">
          <FaPlus /> Add Customer
        </button>
      </div>
    </div>
  );
};

export default CustomerDashboard;
