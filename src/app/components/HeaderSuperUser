"use client"; 

import React from 'react';

const HeaderSuperUser = () => {
  return (
    <div className="w-full flex items-center justify-between p-4 bg-[#003366] shadow-lg">
      {/* Left side: HyperIn Care logo */}
      <div className="text-white text-lg font-semibold">
        HyperIn<span className="text-white"> Care | Service Admin</span>
      </div>

      {/* Right side: User Info */}
      <div className="flex items-center space-x-2">
        <div className="text-white text-sm">Admin: Chris Anderson</div>
        <div className="w-10 h-10 flex items-center justify-center rounded-full bg-blue-500 text-white hover:border-green-600 border-2">
         CA
        </div>
      </div>
    </div>
  );
};

export default HeaderSuperUser;
